pub struct Unit {
    unit_type_index: u8,
    moves_remaining: u8,
    hits_remaining: u8,
    territory_index: u8,
    connection_index: u8,
    unit_after_move_index: u8,
    unit_after_hit_index: u8,
    unit_after_turn_index: u8,
    //offset_index: u8,
}
